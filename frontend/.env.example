# URL of the deployed backend (see ../backend). Leave empty for local dev if
# you're running the backend on the same origin via a proxy; otherwise point
# this at http://localhost:3000 (local) or the Render URL (deployed).
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
