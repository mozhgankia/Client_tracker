# راهنمای دیپلوی کامل (از صفر تا اپ زنده)

سه سرویس داریم که به هم وصل می‌شوند. ترتیب مهم است، چون هر کدام به آدرس قبلی نیاز دارد:

```
Supabase (دیتابیس)  ←  Render (بک‌اند/API)  ←  Vercel (فرانت‌اند/داشبورد)
        رایگان                رایگان                  رایگان
```

## نقشه‌ی راه (ترتیب کارها)

| مرحله | کجا | نتیجه‌ای که می‌گیرید | راهنمای کامل |
|---|---|---|---|
| ۱ | **Supabase** | `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` | [`backend/DEPLOY_SUPABASE.md`](./backend/DEPLOY_SUPABASE.md) |
| ۲ | **Render** (بک‌اند) | آدرس بک‌اند، مثلاً `https://…​onrender.com` | [`backend/DEPLOY_RENDER.md`](./backend/DEPLOY_RENDER.md) |
| ۳ | **Vercel** (فرانت‌اند) | آدرس داشبورد، مثلاً `https://…​vercel.app` | [`frontend/DEPLOY_VERCEL.md`](./frontend/DEPLOY_VERCEL.md) |
| ۴ | **سیم‌کشی نهایی** | وصل‌کردن آدرس‌ها به همدیگر (پایین) | همین فایل |

## چرا این ترتیب؟ (زنجیره‌ی وابستگی)

- بک‌اند (مرحله ۲) به کلیدهای **Supabase** (مرحله ۱) نیاز دارد → پس اول Supabase.
- فرانت‌اند (مرحله ۳) باید بداند API کجاست، یعنی به آدرس **Render** (مرحله ۲) نیاز دارد → پس Render قبل از Vercel.
- بک‌اند باید بداند به کدام فرانت‌اند اجازه‌ی دسترسی بدهد (CORS)، یعنی به آدرس **Vercel** (مرحله ۳) نیاز دارد → این تنها چیزی است که «برگشتی» است و در مرحله ۴ تنظیمش می‌کنیم.

## مرحله ۴ — سیم‌کشی نهایی (جایی که همه گیر می‌کنند)

بعد از اینکه هر سه سرویس بالا آمدند، سه متغیر محیطی باید به هم اشاره کنند:

| متغیر | کجا تنظیم می‌شود | مقدارش چیست |
|---|---|---|
| `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` | Render | از مرحله ۱ |
| `NEXT_PUBLIC_API_BASE_URL` | Vercel | آدرس **Render** (مرحله ۲) |
| `PUBLIC_URL` | Render | آدرس **خود Render** (بعد از اولین دیپلوی معلوم می‌شود) |
| `CORS_ORIGIN` | Render | آدرس **Vercel** (مرحله ۳) |

**دو متغیر آخر (`PUBLIC_URL` و `CORS_ORIGIN`) را نمی‌توانید در اولین دیپلوی پر کنید**، چون آدرس‌هایشان هنوز وجود ندارد. پس:
1. اول Render را با `SUPABASE_*` + `JWT_SECRET` دیپلوی کنید → آدرس Render را بگیرید → همان را در `PUBLIC_URL` بگذارید.
2. بعد Vercel را با `NEXT_PUBLIC_API_BASE_URL` = آدرس Render دیپلوی کنید → آدرس Vercel را بگیرید.
3. برگردید به Render و `CORS_ORIGIN` = آدرس Vercel را بگذارید و ذخیره کنید.

## تست نهایی (بعد از سیم‌کشی)

1. آدرس Vercel را در مرورگر باز کنید → باید صفحه‌ی **ورود** (فارسی، راست‌به‌چپ) را ببینید.
2. **ثبت‌نام** کنید → باید مستقیم وارد داشبورد شوید.
3. یک **مشتری جدید** بسازید → اگر بلافاصله در لیست ظاهر شد، یعنی کل زنجیره‌ی **Vercel → Render → Supabase** زنده است. 🎉
4. اتصال **واتساپ/تلگرام** را از تب «اتصالات» تست کنید (اینجا دیگر محدودیت شبکه‌ی سندباکس را نداریم، پس QR/کد واقعی می‌آید).

## اگر چیزی کار نکرد — عیب‌یابی سریع

| نشانه | علت محتمل | راه حل |
|---|---|---|
| دیپلوی Vercel با خطای `No Output Directory named "out" found` شکست می‌خورد | `Root Directory` در Vercel روی `frontend` تنظیم نشده (Vercel `package.json` ریشه را می‌بیند) | Vercel → Settings → General → Root Directory = `frontend` → Save → Redeploy |
| صفحه‌ی ورود می‌آید ولی ثبت‌نام خطای «Failed to fetch» می‌دهد | `NEXT_PUBLIC_API_BASE_URL` در Vercel اشتباه/خالی است | آدرس Render را درست وارد و دوباره Deploy کنید |
| خطای CORS در Console مرورگر | `CORS_ORIGIN` در Render آدرس Vercel را ندارد | آدرس دقیق Vercel را در `CORS_ORIGIN` بگذارید |
| اولین درخواست خیلی کند است (چند ثانیه) | سرویس رایگان Render خوابیده بوده | طبیعی است؛ پینگ خودکار ما بعد از ست‌شدن `PUBLIC_URL` جلویش را می‌گیرد |
| خطای اتصال به دیتابیس | `SUPABASE_*` اشتباه است یا `schema.sql` اجرا نشده | کلیدها را دوباره چک کنید و مطمئن شوید جدول‌ها در Supabase ساخته شده‌اند |

> جزئیات کامل هر مرحله در سه فایل راهنمای جدول بالاست. این فایل فقط نقشه‌ی کلی و سیم‌کشی بین سرویس‌هاست.
